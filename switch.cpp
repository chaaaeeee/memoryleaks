#include <iostream>

int main() {
    do{
        int menu;
        std::cin >> menu;
        switch(menu) {
            case 1:
                std::cout << "tes";
                break;
            case 2:
                std::cout << "set";
                break;
        }
    }while(true);
}
